from robot_washing_expert_library.common import CommonLibrary


class LoginLibrary(CommonLibrary):
    def login(self, username, password):
        return super(LoginLibrary, self).login(username, password)

    def refurbish(self):
        return super(LoginLibrary, self).refurbish()
