{
  "type": "object",
  "required": [
    "first_order_count",
    "register_count",
    "follow_count"
  ],
  "properties": {
    "first_order_count": { "type": "number" },
    "register_count": { "type": "number" },
    "follow_count": { "type": "number" }
  }
}